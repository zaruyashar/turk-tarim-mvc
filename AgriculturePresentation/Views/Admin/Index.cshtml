@using EntityLayer.Concrete
@model List<Admin>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Admin Listesi</h4>
            <div class="table-responsive pt-3">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Kullanıcı Adı</th>
                            <th>Sil</th>
                            <th>Düzenle</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.AdminID</td>
                                <td>@item.Username</td>
                                <td><a asp-controller="Admin" asp-action="DeleteAdmin" asp-route-id="@item.AdminID" class="btn btn-outline-danger">Sil</a></td>
                                <td><a asp-controller="Admin" asp-action="EditAdmin" asp-route-id="@item.AdminID" class="btn btn-outline-success">Güncelle</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
                <br />
                <a asp-controller="Admin" asp-action="AddAdmin" class="btn btn-outline-info">Yeni Admin Girişi</a>
            </div>
        </div>
    </div>
</div>